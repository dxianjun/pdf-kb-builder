interface:
  display_name: "PDF KB Builder"
  short_description: "Build or reindex PDF knowledge bases"
  default_prompt: "Build a searchable knowledge base from PDFs in a directory using MarkItDown first, or reindex/cross-check an existing .pdf_kb/markdown directory without rerunning MarkItDown."
